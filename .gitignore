.venv/
venv/
__pycache__/
*.pyc
.streamlit/secrets.toml
.DS_Store

# dbt build artifacts -- regenerated by `dbt seed && dbt run`, not source.
# dbt/running.duckdb IS committed on purpose: it's what the deployed app
# queries, and Streamlit Cloud doesn't run a dbt build step.
dbt/target/
dbt/logs/
dbt/dbt_packages/
dbt/.user.yml
